services:
  db:
    image: mysql:lts-oracle
    mem_limit: 512m
    container_name: db
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: "dev"
      MYSQL_DATABASE: findahelper
    ports:
      - "3306:3306"
    volumes:
      - db_data:/var/lib/mysql

volumes:
  db_data:
